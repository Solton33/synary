<div class="contents row">
  <div class="container">
    <h3>投稿する</h3>
    <%= form_with(model: @scenario, local: true) do |form| %>
      <%= form.text_field :title, placeholder: "タイトル" %>
      <%= form.text_field :image, placeholder: "画像投稿" %>
      <%= form.text_area :scenario, placeholder: "テキスト", rows: "10" %>
      <%= form.submit "投稿" %>
    <% end %>
  </div>
</div>